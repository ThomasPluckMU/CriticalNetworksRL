trainer: "SingleGameTrainer"
game: "ALE/Pong-v5"
device: "cuda:1"
episodes: 5000
render: false
debug: true
save_dir: "logs"
log_dir: "logs"

agents:
  CriticalAgent:
    agent_params:
      epsilon: [0.0, 0.05, 0.1]
      reg_strength: [0.0001, 0.001, 0.01]
    logics:
      SARSALogic:
        logic_params:
          gamma: [0.9, 0.99, 0.999]
