trainer: "SingleGameTrainer"
game: "ALE/Pong-v5"
device: "cuda:0"
episodes: 5000
render: false
debug: true
save_dir: "logs"
log_dir: "logs"

agents:
  StandardAtariDQN:
    agent_params:
      epsilon: [0.0, 0.02]
    logics:
      TDLogic:
        logic_params:
          gamma: [0.9, 0.99, 0.999]

  CriticalAgent:
    agent_params:
      epsilon: [0.0, 0.02]
      reg_strength: [0.0001, 0.001, 0.01]
    logics:
      TDLogic:
        logic_params:
          gamma: [0.9, 0.99, 0.999]
